[alias]
addf = "!git ls-files -m -o --exclude-standard | fzf --print0 -m | xargs -0 -t -o git add"
[pull]
	rebase = false
[oh-my-zsh]
	hide-status = 1
	hide-dirty = 1
	hide-status = 0
	hide-dirty = 0
	hide-status = 0
	hide-dirty = 0
