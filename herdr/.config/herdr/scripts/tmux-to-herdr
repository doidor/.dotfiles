#!/bin/sh

set -eu

if ! command -v python3 >/dev/null 2>&1; then
    printf 'tmux-to-herdr: python3 is required\n' >&2
    exit 1
fi

script_dir=$(CDPATH= cd "$(dirname "$0")" && pwd)
exec python3 "$script_dir/../lib/tmux_to_herdr.py" "$@"
